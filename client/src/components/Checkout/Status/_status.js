import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  statusText: {
    fontSize: '20px',
    fontWeight: 'bold'
  },
  imgDelivery: {
    width: '100px'
  }
}));

export default useStyles;
